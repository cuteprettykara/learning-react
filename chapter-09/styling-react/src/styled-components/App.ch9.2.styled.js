import React, { Component } from 'react';
import StyledButton from './StyledButton.2';

class App extends Component {
  render() {

    return (
      <div>
        <StyledButton>버튼</StyledButton>
      </div>
    );
  }
}

export default App;
